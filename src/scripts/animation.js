const animation = function () {
    // const animationTarget = $('#about-visual img'),
    //     randNum = Math.floor(Math.random() * 100);
    //
    // let animationTimer = setTimeout(function move() {
    //     animationTarget.style.right = randNum + 'px';
    //     animationTimer = setTimeout(move, 4000);
    // }, 4000);
    //
    // animationTimer();
};

$(document).ready(animation);